{
  "name": "@saransh/frontend",
  "version": "1.0.0",
  "private": true,
  "description": "Saransh Frontend - React/Next.js frontend for AI-powered news aggregation",
  "scripts": {
    "build": "echo 'Frontend build placeholder - configure when frontend is added'",
    "dev": "echo 'Frontend dev placeholder - configure when frontend is added'",
    "lint": "echo 'Frontend lint placeholder - configure when frontend is added'",
    "test": "echo 'Frontend test placeholder - configure when frontend is added'",
    "clean": "rm -rf dist build .next node_modules/.cache || true"
  }
}
